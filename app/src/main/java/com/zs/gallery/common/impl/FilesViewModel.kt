package com.zs.gallery.common.impl

import androidx.lifecycle.ViewModel
import com.zs.gallery.files.FilesViewState

class FilesViewModel : ViewModel(), FilesViewState {

    override fun onCleared() {
        super.onCleared()
    }
}